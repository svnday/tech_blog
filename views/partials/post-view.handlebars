<div class="card">
    <div class="card-header">
        <h4><a class="link" href="/post/{{id}}">{{title}}</a><p class="postedBy">Posted by {{user.username}} on {{format_date created_at}}</p></h4>
    </div>
        <p class="card-body"></p>{{content}}</p>
</div>